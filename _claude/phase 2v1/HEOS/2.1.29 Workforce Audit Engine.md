# WHYCESPACE WBSM v3
# HEOS — HUMAN ECONOMIC OPERATING SYSTEM
# 2.1.29 WORKFORCE AUDIT ENGINE (CANONICAL IMPLEMENTATION PROMPT)

You are implementing the **Workforce Audit Engine**.

This engine produces **audit records for workforce actions and lifecycle events** within the HEOS system.

Audit evaluation supports:

• operational transparency  
• compliance tracking  
• governance oversight  
• historical analysis  

The engine generates **deterministic audit entries** for workforce-related operations.

Examples of audited actions:

• workforce registration  
• lifecycle transitions  
• capability changes  
• availability updates  
• assignment decisions  
• scheduling events  
• incentive decisions  

The engine must **NOT mutate domain aggregates**.

It produces **audit output only**.

Domain models referenced:

src/domain/core/workforce/

This implementation MUST follow:

• WBSM v3 Prompt Guardrails  
• WBSM v3 Prompt Structural Rules  
• Canonical Repository Structure  

If any rule cannot be satisfied you must STOP and request clarification.

---

# STEP 1 — ARCHITECTURE VERIFICATION

Component classification:

Workforce Audit Engine = **Intelligence Engine**

Correct layer:

src/engines/T3I/

Reason:

Audit engines generate analytical outputs and audit records.

They do not perform operational mutations.

Therefore it belongs in **T3I Intelligence Engines**.

Engine responsibilities:

• construct audit records  
• normalize audit data  
• produce deterministic audit output  

---

# STEP 2 — TARGET FILE PATH

Create the HEOS intelligence engine module.

Target location:

src/engines/T3I/HEOS/

Files to create:

WorkforceAuditEngine.cs  
WorkforceAuditCommand.cs  
WorkforceAuditRecord.cs  

Before generating code you MUST:

1. Print the target file path  
2. Verify the path matches canonical architecture  
3. Confirm correct layer placement  

If mismatch occurs:

STOP and request clarification.

---

# STEP 3 — TARGET DIRECTORY STRUCTURE

Confirm directory structure before generating code.

```
src/engines/T3I/HEOS/

WorkforceAuditEngine.cs
WorkforceAuditCommand.cs
WorkforceAuditRecord.cs
```

If the directory does not exist create it exactly as defined.

You must NOT:

• create new top-level directories  
• move architectural layers  
• invent folder structures  

---

# STEP 4 — COMMAND MODEL

Create immutable command model.

WorkforceAuditCommand

Fields:

WorkforceId  
ActionType  
ActionReferenceId  
PerformedBy  
Timestamp  
Details  

ActionType examples:

WorkforceRegistered  
CapabilityAssigned  
AvailabilityChanged  
AssignmentCreated  
ScheduleCreated  
LifecycleChanged  
IncentiveEvaluated  

ActionReferenceId links to the related operational record.

Command models must:

• be immutable  
• contain no domain logic  
• represent engine input only

Timestamp must be provided by the caller.

The engine must not generate system time.

---

# STEP 5 — AUDIT RECORD MODEL

Create:

WorkforceAuditRecord

Fields:

AuditId  
WorkforceId  
ActionType  
ActionReferenceId  
PerformedBy  
Timestamp  
AuditSummary  

AuditId must be **deterministically generated** using command inputs (for example WorkforceId + ActionReferenceId + Timestamp) or provided externally.

AuditSummary describes the recorded event.

Audit records must be immutable.

Decision models must:

• contain no domain aggregates  
• contain no runtime logic  
• represent engine output only

---

# STEP 6 — ENGINE IMPLEMENTATION

Create:

WorkforceAuditEngine.cs

Responsibilities:

• construct audit records for workforce actions  
• normalize audit data  
• generate deterministic audit output  
• return audit record for persistence by external systems

Example method signature:

GenerateAuditRecord(
    WorkforceAggregate workforce,
    WorkforceAuditCommand command)

Return:

WorkforceAuditRecord

Engine requirements:

• stateless  
• thread-safe  
• deterministic  

The engine must NOT:

• persist audit data  
• call other engines  
• invoke infrastructure  
• mutate domain aggregates  

Persistence must be handled by runtime or infrastructure layers.

---

# STEP 7 — AUDIT RULES

The engine must enforce:

Timestamp must be valid.

ActionType must be recognized.

WorkforceId must be present.

ActionReferenceId must be valid when provided.

Audit records must be immutable.

Audit output must be deterministic.

Unknown action types must be rejected.

---

# STEP 8 — ENGINE INVOCATION RULE

Engines must never call other engines.

Correct invocation flow:

Runtime Dispatcher → WorkforceAuditEngine

Incorrect flow:

WorkforceAuditEngine → another engine

Runtime orchestration must invoke engines.

---

# STEP 9 — UNIT TESTS

Create deterministic tests.

Target location:

tests/decision-engines/

File:

WorkforceAuditEngineTests.cs

Test scenarios:

• audit record generation  
• invalid timestamp rejection  
• missing workforce id rejection  
• unknown action type rejection  
• deterministic audit output validation  

All tests must pass.

---

# STEP 10 — DEBUG ENDPOINT

Expose inspection endpoint.

Location:

src/platform/controlplane/

Example endpoint:

POST /dev/heos/workforce/audit

Platform controllers must NOT invoke engines directly.

Correct flow:

Platform Controller  
      ↓
Runtime Dispatcher  
      ↓
WorkforceAuditEngine

---

# STEP 11 — VALIDATION CHECKLIST

Before completing implementation confirm:

• No domain models created inside engines  
• Engine is stateless  
• No persistence logic inside engine  
• No engine-to-engine calls  
• Audit output immutable  

---

# SUCCESS CRITERIA

Implementation is valid when:

• Build succeeds  
• 0 warnings  
• 0 errors  
• All tests pass  
• Architecture remains unchanged

---

# STATUS

HEOS Workforce Audit Engine  
WBSM v3 IMPLEMENTATION PROMPT