# WHYCESPACE WBSM v3
# COMPONENT IMPLEMENTATION PROMPT

Component Number:
2.0.43

Component Name:
Evidence Hash Engine

System Category:
WhyceChain System

Architecture Layer:
Engines Layer

Engine Tier:
T0U — Constitutional Engine

Implementation Category:
EXECUTION ENGINE

------------------------------------------------------------
SECTION 1 — ARCHITECTURE VERIFICATION
------------------------------------------------------------

You are implementing the **Evidence Hash Engine** for the WhyceChain system.

WhyceChain provides **immutable evidence recording** for all critical system actions within Whycespace.

Evidence hashing ensures that:

• ledger entries are tamper-proof
• governance decisions are verifiable
• workflow outcomes are immutable
• block hashes remain deterministic
• Merkle roots can be calculated reliably

This engine produces **cryptographic hashes** for evidence artifacts.

Examples of evidence inputs:

• policy decision records
• identity verification events
• workflow execution outputs
• ledger entries
• block structures
• governance actions

The engine belongs to the **T0U Constitutional Engine tier** because evidence hashing underpins the **constitutional trust guarantees** of the entire system.

------------------------------------------------------------
SECTION 2 — REPOSITORY LOCATION VERIFICATION
------------------------------------------------------------

Target engine path:

src/engines/T0U/WhyceChain/

File:

EvidenceHashEngine.cs

Associated system models:

src/system/upstream/WhyceChain/ledger/

Associated tests:

tests/whycechain-evidence/

Allowed dependency direction:

domain
↓
system
↓
engines
↓
runtime
↓
platform
↓
infrastructure

The engine may reference **system-layer ledger models** but must not depend on runtime or infrastructure layers.

------------------------------------------------------------
SECTION 3 — ENGINE RULES
------------------------------------------------------------

The Evidence Hash Engine must be:

• stateless
• deterministic
• thread-safe

The engine must NOT:

• persist data
• access infrastructure
• publish events
• invoke other engines

Execution contract:

EngineInput
→ EngineExecution
→ EngineResult

------------------------------------------------------------
SECTION 4 — ENGINE INPUT MODEL
------------------------------------------------------------

Create model:

EvidenceHashCommand.cs

Fields:

EvidenceType (string)

EvidencePayload (object)

TraceId (string)

CorrelationId (string)

Timestamp (DateTime)

Evidence types may include:

PolicyDecision
IdentityVerification
WorkflowOutcome
LedgerEntry
ChainBlock
GovernanceAction

------------------------------------------------------------
SECTION 5 — ENGINE RESULT MODEL
------------------------------------------------------------

Create model:

EvidenceHashResult.cs

Fields:

EvidenceHash (string)

HashAlgorithm (string)

PayloadCanonicalHash (string)

MetadataHash (string)

GeneratedAt (DateTime)

TraceId (string)

------------------------------------------------------------
SECTION 6 — HASHING RULES
------------------------------------------------------------

The engine must generate deterministic hashes using:

Algorithm:

SHA256

Canonical serialization rules:

• JSON serialization
• deterministic property ordering
• UTF-8 encoding

Hash generation must include:

payload hash
metadata hash
combined evidence hash

EvidenceHash = SHA256(payloadHash + metadataHash)

------------------------------------------------------------
SECTION 7 — ENGINE EXECUTION
------------------------------------------------------------

Implement engine:

EvidenceHashEngine.cs

Primary method:

Execute(EvidenceHashCommand command)

Steps:

1. canonicalize payload
2. compute payload hash
3. compute metadata hash
4. compute final evidence hash
5. return EvidenceHashResult

All operations must be deterministic.

------------------------------------------------------------
SECTION 8 — SECURITY REQUIREMENTS
------------------------------------------------------------

The engine must ensure:

• consistent hash generation across nodes
• collision-resistant hashing
• canonical serialization
• deterministic ordering

No randomness allowed.

------------------------------------------------------------
SECTION 9 — TEST REQUIREMENTS
------------------------------------------------------------

Tests must validate:

• deterministic hash generation
• identical payload produces identical hash
• metadata affects final hash
• concurrency safety
• performance under high load

Test files:

EvidenceHashEngineTests.cs

Test scenarios:

hash stability test
payload mutation detection
metadata variation test
concurrency hashing test

Test location:

tests/whycechain-evidence/

All builds must pass:

0 warnings
0 errors

------------------------------------------------------------
SECTION 10 — DEBUG ENDPOINT
------------------------------------------------------------

Create developer debug endpoint.

Endpoint:

/dev/chain/hash

Capabilities:

• generate hash for payload
• verify deterministic hashing
• inspect hash components

Endpoint must exist only in development builds.

------------------------------------------------------------
SECTION 11 — PROMPT STORAGE
------------------------------------------------------------

Claude Code must store the prompt file.

Directory:

claude/phase2x/prompts/

Filename:

2.0.43-evidence-hash-engine.prompt.md

Rules:

• filename must start with component number
• lowercase naming
• hyphen-separated words

------------------------------------------------------------
SECTION 12 — VALIDATION CHECKLIST
------------------------------------------------------------

Before completing implementation verify:

[x] correct engine tier (T0U)
[x] stateless engine implementation
[x] deterministic hashing enforced
[x] canonical serialization implemented
[x] no infrastructure dependencies
[x] tests implemented
[x] debug endpoint created

------------------------------------------------------------
SECTION 13 — FINAL RULE
------------------------------------------------------------

You are operating under **Whycespace WBSM v3 strict architecture mode**.

If any requested implementation violates canonical architecture:

STOP and request clarification.

No partial implementations allowed.
