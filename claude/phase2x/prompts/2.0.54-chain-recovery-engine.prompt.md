# WHYCESPACE WBSM v3
# COMPONENT IMPLEMENTATION PROMPT

Component Number:
2.0.54

Component Name:
Chain Recovery Engine

System Category:
WhyceChain System

Architecture Layer:
Engines Layer

Engine Tier:
T3I — Intelligence Engine

Implementation Category:
INTELLIGENCE ENGINE

------------------------------------------------------------
SECTION 1 — ARCHITECTURE VERIFICATION
------------------------------------------------------------

You are implementing the **Chain Recovery Engine** for the WhyceChain system.

WhyceChain provides immutable evidence logging for governance operations in the Whycespace ecosystem.

The Chain Recovery Engine reconstructs the chain state using:

• chain snapshots
• replicated block data
• ledger entry streams

This engine allows nodes or clusters to restore chain state after:

• node restart
• data corruption
• disaster recovery
• cluster bootstrap

The engine produces recovery plans and reconstructed chain structures but does NOT persist or transport data.

------------------------------------------------------------
SECTION 2 — REPOSITORY LOCATION VERIFICATION
------------------------------------------------------------

Target engine path:

src/engines/T3I/WhyceChain/

File:

ChainRecoveryEngine.cs

Associated system models:

src/system/upstream/WhyceChain/ledger/

Required models:

ChainLedgerEntry
ChainBlock

Associated tests:

tests/whycechain-recovery/

Allowed dependency direction:

domain
↓
system
↓
engines
↓
runtime
↓
platform
↓
infrastructure

The engine may reference system models but must not depend on runtime or infrastructure layers.

------------------------------------------------------------
SECTION 3 — ENGINE RULES
------------------------------------------------------------

The Chain Recovery Engine must be:

• stateless
• deterministic
• thread-safe

The engine must NOT:

• persist recovered data
• access infrastructure
• modify original chain data
• call other engines

Execution contract:

EngineInput
→ EngineExecution
→ EngineResult

------------------------------------------------------------
SECTION 4 — ENGINE INPUT MODEL
------------------------------------------------------------

Create model:

ChainRecoveryCommand.cs

Fields:

SnapshotHeight (long)

SnapshotBlockHash (string)

ReplicatedBlocks (IReadOnlyList<ChainBlock>)

ReplicatedLedgerEntries (IReadOnlyList<ChainLedgerEntry>)

TraceId (string)

CorrelationId (string)

Timestamp (DateTime)

Rules:

• snapshot height must correspond to a valid block
• replicated blocks must start at snapshot height

------------------------------------------------------------
SECTION 5 — ENGINE RESULT MODEL
------------------------------------------------------------

Create model:

ChainRecoveryResult.cs

Fields:

RecoveredBlocks (IReadOnlyList<ChainBlock>)

RecoveredEntries (IReadOnlyList<ChainLedgerEntry>)

RecoveredHeight (long)

RecoveryHash (string)

RecoveredEntryCount (int)

GeneratedAt (DateTime)

TraceId (string)

------------------------------------------------------------
SECTION 6 — RECOVERY RULES
------------------------------------------------------------

The recovery engine must:

1. validate snapshot block hash
2. verify replicated block linkage
3. reconstruct chain blocks after snapshot
4. rebuild ledger entry stream
5. confirm deterministic ordering

RecoveryHash must be computed using SHA256 over:

RecoveredHeight
RecoveredEntryCount
LatestRecoveredBlockHash

Serialization must be deterministic.

------------------------------------------------------------
SECTION 7 — ENGINE EXECUTION
------------------------------------------------------------

Implement engine:

ChainRecoveryEngine.cs

Primary method:

Execute(ChainRecoveryCommand command)

Execution steps:

1. validate snapshot integrity
2. validate replicated block linkage
3. reconstruct chain blocks
4. rebuild ledger entries
5. compute recovery entry count
6. compute recovery hash
7. return ChainRecoveryResult

The engine must not modify original chain data.

------------------------------------------------------------
SECTION 8 — SECURITY REQUIREMENTS
------------------------------------------------------------

The engine must guarantee:

• deterministic recovery results
• correct block linkage reconstruction
• consistent recovery hash across nodes

No randomness allowed.

------------------------------------------------------------
SECTION 9 — TEST REQUIREMENTS
------------------------------------------------------------

Tests must validate:

• correct chain reconstruction
• snapshot validation
• replicated block validation
• deterministic recovery hash generation
• recovery failure detection

Test files:

ChainRecoveryEngineTests.cs

Test scenarios:

valid recovery test
snapshot mismatch detection
invalid block linkage detection
recovery hash stability test

Test location:

tests/whycechain-recovery/

Build requirements:

0 warnings
0 errors

------------------------------------------------------------
SECTION 10 — DEBUG ENDPOINT
------------------------------------------------------------

Create developer debug endpoint.

Endpoint:

/dev/chain/recover

Capabilities:

• simulate chain recovery
• inspect recovery payload
• verify recovery hash

Endpoint must exist only in development builds.

------------------------------------------------------------
SECTION 11 — PROMPT STORAGE
------------------------------------------------------------

Claude Code must store the prompt file.

Directory:

claude/phase2x/prompts/

Filename:

2.0.54-chain-recovery-engine.prompt.md

Rules:

• filename begins with component number
• lowercase naming
• hyphen-separated words

------------------------------------------------------------
SECTION 12 — VALIDATION CHECKLIST
------------------------------------------------------------

Before completing implementation verify:

[x] stateless engine implementation
[x] deterministic recovery process
[x] recovery hash generation implemented
[x] read-only chain processing
[x] no infrastructure dependencies
[x] tests implemented
[x] debug endpoint created

------------------------------------------------------------
SECTION 13 — FINAL RULE
------------------------------------------------------------

You are operating under **Whycespace WBSM v3 strict architecture mode**.

If any requested implementation violates canonical architecture:

STOP and request clarification.

No partial implementations allowed.
