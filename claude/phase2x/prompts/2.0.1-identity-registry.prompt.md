# WHYCESPACE WBSM v3
# WHYCEID SYSTEM
# COMPONENT 2.0.1
# IDENTITY REGISTRY

This prompt must be executed using the Whycespace MASTER PROMPT GENERATION TEMPLATE.

This prompt implements the foundational identity registry used across the entire Whycespace platform.

All implementation must follow the canonical architecture defined in:

claude/bootstrap-prompts/master-bootstrap.prompt.md

and related architecture standards.

------------------------------------------------------------
SECTION 1 — COMPONENT IDENTIFICATION
------------------------------------------------------------

Component Number:
2.0.1

Component Name:
Identity Registry

System Category:
WhyceID System

Architecture Layer:
System Layer

Engine Tier:
N/A

Component Type:
SYSTEM MODEL + REGISTRY STORE

------------------------------------------------------------
SECTION 2 — ARCHITECTURE VERIFICATION
------------------------------------------------------------

Identity Registry belongs to the SYSTEM LAYER.

This component stores identity records used by:

Authentication Engine
Authorization Engine
Identity Graph Engine
TrustScore Engine
Device Trust Engine
Federation Engine

Identity Registry must NOT contain runtime logic.

It must only provide:

• identity record storage
• identity lookup
• identity indexing
• identity lifecycle state

------------------------------------------------------------
SECTION 3 — TARGET REPOSITORY PATHS
------------------------------------------------------------

Primary implementation path:

src/system/upstream/WhyceID/registry/

Required folders:

src/system/upstream/WhyceID/registry
src/system/upstream/WhyceID/models
src/system/upstream/WhyceID/stores

If folders do not exist Claude Code may create them.

Associated test project:

tests/whyceid-identity/

------------------------------------------------------------
SECTION 4 — DOMAIN MODEL REFERENCES
------------------------------------------------------------

Domain identity models already exist under:

src/domain/core/identity/

The registry must reference domain primitives but must not define domain models.

------------------------------------------------------------
SECTION 5 — IDENTITY MODEL
------------------------------------------------------------

Create model:

IdentityRecord.cs

Location:

src/system/upstream/WhyceID/models/

Fields:

IdentityId (Guid)
IdentityType (User | Operator | Provider | Service)
Status (Active | Suspended | Revoked | Pending)
CreatedAt (DateTime)
UpdatedAt (DateTime)
PrimaryEmail (optional)
PrimaryPhone (optional)
ExternalReferenceId (optional)

Record must be immutable.

------------------------------------------------------------
SECTION 6 — IDENTITY REGISTRY CONTRACT
------------------------------------------------------------

Create interface:

IIdentityRegistry.cs

Location:

src/system/upstream/WhyceID/registry/

Methods:

RegisterIdentity(IdentityRecord record)

GetIdentity(Guid identityId)

GetIdentityByEmail(string email)

GetIdentityByPhone(string phone)

UpdateIdentityStatus(Guid identityId, IdentityStatus status)

ListIdentities(int page, int pageSize)

------------------------------------------------------------
SECTION 7 — REGISTRY STORE IMPLEMENTATION
------------------------------------------------------------

Create store implementation:

IdentityRegistryStore.cs

Location:

src/system/upstream/WhyceID/stores/

Responsibilities:

• persistent identity storage
• indexed lookup
• thread-safe access

Storage backend:

PostgreSQL adapter (initial implementation may use in-memory store for development).

------------------------------------------------------------
SECTION 8 — INDEXING REQUIREMENTS
------------------------------------------------------------

Identity registry must support indexed lookup by:

IdentityId
PrimaryEmail
PrimaryPhone

Indexes must be deterministic.

------------------------------------------------------------
SECTION 9 — LIFECYCLE STATES
------------------------------------------------------------

Identity lifecycle states:

Pending
Active
Suspended
Revoked

State transitions must be validated by future engines.

Registry must not enforce lifecycle rules.

------------------------------------------------------------
SECTION 10 — TEST IMPLEMENTATION
------------------------------------------------------------

Create tests in:

tests/whyceid-identity/

File:

IdentityRegistryTests.cs

Test cases:

Register identity
Retrieve identity by id
Retrieve identity by email
Retrieve identity by phone
Update identity status
Pagination list

Concurrency tests:

100 concurrent registrations
parallel reads

------------------------------------------------------------
SECTION 11 — DEBUG ENDPOINT
------------------------------------------------------------

Add development debug endpoint.

Location:

src/engines/T4A/developer/

Endpoint:

GET /dev/identity/{id}

Response:

IdentityRecord

Endpoint must only be available in development builds.

------------------------------------------------------------
SECTION 12 — VALIDATION CHECKLIST
------------------------------------------------------------

Before completing implementation verify:

[ ] repository paths correct
[ ] system layer respected
[ ] no runtime logic included
[ ] thread-safe store
[ ] indexes implemented
[ ] tests created
[ ] build succeeds
[ ] 0 warnings
[ ] 0 errors

------------------------------------------------------------
SECTION 13 — PROMPT STORAGE
------------------------------------------------------------

Claude Code must store this prompt file.

Storage location:

claude/phase2x/prompts/

File name:

2.0.1-identity-registry.prompt.md

If the folder does not exist create it.

------------------------------------------------------------
SECTION 14 — FINAL RULE
------------------------------------------------------------

This implementation must comply with:

Whycespace WBSM v3 architecture

If any architecture rule is violated:

STOP and request clarification.
