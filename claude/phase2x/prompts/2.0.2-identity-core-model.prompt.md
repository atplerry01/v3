# WHYCESPACE WBSM v3
# WHYCEID SYSTEM
# COMPONENT 2.0.2
# IDENTITY CORE MODEL

This prompt must be executed using the Whycespace MASTER PROMPT GENERATION TEMPLATE.

This component defines the canonical Identity Core Model used across the entire Whycespace platform.

The model represents the fundamental identity structure used by:

Authentication Engine
Authorization Engine
TrustScore Engine
Device Trust Engine
Identity Graph Engine
Federation Engine

All implementations must follow the canonical architecture defined in:

claude/bootstrap-prompts/master-bootstrap.prompt.md

and related architecture standards.

------------------------------------------------------------
SECTION 1 — COMPONENT IDENTIFICATION
------------------------------------------------------------

Component Number:
2.0.2

Component Name:
Identity Core Model

System Category:
WhyceID System

Architecture Layer:
System Layer

Engine Tier:
N/A

Component Type:
SYSTEM MODEL

------------------------------------------------------------
SECTION 2 — ARCHITECTURE VERIFICATION
------------------------------------------------------------

Identity Core Model belongs to the SYSTEM LAYER.

It defines the canonical identity structure used throughout Whycespace.

This model must:

• represent identity attributes
• support identity lifecycle
• support multi-role identities
• support verification state
• support trust scoring
• support federation references

The model must NOT contain runtime logic.

It must only define the structure of identity.

------------------------------------------------------------
SECTION 3 — TARGET REPOSITORY PATH
------------------------------------------------------------

Primary implementation location:

src/system/upstream/WhyceID/models/

Create file:

Identity.cs

Associated models may also be created within the same folder.

Associated test project:

tests/whyceid-identity/

------------------------------------------------------------
SECTION 4 — DOMAIN MODEL REFERENCES
------------------------------------------------------------

Domain primitives may exist under:

src/domain/core/identity/

The system model may reference domain primitives but must not duplicate domain structures.

------------------------------------------------------------
SECTION 5 — IDENTITY TYPE ENUM
------------------------------------------------------------

Create enumeration:

IdentityType.cs

Location:

src/system/upstream/WhyceID/models/

Values:

User
Operator
Provider
Service
System

------------------------------------------------------------
SECTION 6 — IDENTITY STATUS ENUM
------------------------------------------------------------

Create enumeration:

IdentityStatus.cs

Values:

Pending
Active
Suspended
Revoked

------------------------------------------------------------
SECTION 7 — VERIFICATION STATUS ENUM
------------------------------------------------------------

Create enumeration:

VerificationStatus.cs

Values:

Unverified
Pending
Verified
Rejected

------------------------------------------------------------
SECTION 8 — IDENTITY ROLE MODEL
------------------------------------------------------------

Create model:

IdentityRole.cs

Fields:

RoleId (string)
RoleName (string)
GrantedAt (DateTime)
GrantedBy (Guid)

Purpose:

Defines roles assigned to an identity.

Roles are used by the Authorization Engine.

------------------------------------------------------------
SECTION 9 — IDENTITY ATTRIBUTE MODEL
------------------------------------------------------------

Create model:

IdentityAttribute.cs

Fields:

AttributeKey (string)
AttributeValue (string)
Verified (bool)
CreatedAt (DateTime)

Purpose:

Flexible attribute storage for identity metadata.

------------------------------------------------------------
SECTION 10 — IDENTITY VERIFICATION MODEL
------------------------------------------------------------

Create model:

IdentityVerification.cs

Fields:

VerificationType (Email | Phone | Document | Biometric)
Status (VerificationStatus)
VerifiedAt (DateTime?)
Verifier (string)

Purpose:

Tracks verification state of identity attributes.

------------------------------------------------------------
SECTION 11 — CORE IDENTITY MODEL
------------------------------------------------------------

Create model:

Identity.cs

Fields:

IdentityId (Guid)

IdentityType (IdentityType)

Status (IdentityStatus)

CreatedAt (DateTime)

UpdatedAt (DateTime)

Roles (List<IdentityRole>)

Attributes (List<IdentityAttribute>)

Verifications (List<IdentityVerification>)

TrustScore (double)

ExternalFederationId (string optional)

DeviceTrustEnabled (bool)

SessionEnabled (bool)

Notes:

• model should be immutable where possible
• collections should use read-only wrappers
• no runtime logic allowed

------------------------------------------------------------
SECTION 12 — MODEL RELATIONSHIPS
------------------------------------------------------------

Identity relationships:

Identity
├── Roles
├── Attributes
└── Verifications

These will be used by future engines:

Identity Role Engine
Identity Attribute Engine
Identity Verification Engine
TrustScore Engine

------------------------------------------------------------
SECTION 13 — SERIALIZATION
------------------------------------------------------------

Models must support deterministic serialization.

Use JSON serialization attributes where required.

Ensure compatibility with event sourcing.

------------------------------------------------------------
SECTION 14 — TEST IMPLEMENTATION
------------------------------------------------------------

Create tests in:

tests/whyceid-identity/

File:

IdentityCoreModelTests.cs

Test cases:

Create identity
Add roles
Add attributes
Add verification records
TrustScore value persistence
Serialization validation

Concurrency tests:

Parallel identity creation
Concurrent role assignments

------------------------------------------------------------
SECTION 15 — VALIDATION CHECKLIST
------------------------------------------------------------

Before completing implementation verify:

[x] model contains no runtime logic
[x] system layer respected
[x] domain models not duplicated
[x] immutable structures used
[x] serialization compatibility confirmed
[x] tests created
[x] build succeeds
[x] 0 warnings
[x] 0 errors

------------------------------------------------------------
SECTION 16 — PROMPT STORAGE
------------------------------------------------------------

Claude Code must store this prompt file.

Storage location:

claude/phase2x/prompts/

File name:

2.0.2-identity-core-model.prompt.md

If the directory does not exist Claude Code must create it.

------------------------------------------------------------
SECTION 17 — FINAL RULE
------------------------------------------------------------

This implementation must comply with:

Whycespace WBSM v3 architecture.

If any rule is violated:

STOP and request clarification.
