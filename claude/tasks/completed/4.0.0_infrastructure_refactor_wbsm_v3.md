# 4.0.0 — Infrastructure Refactor (WBSM v3 STRICT)

**Timestamp:** 2026-03-18
**Status:** COMPLETED
**Branch:** refactor/engine-layer-canonical

---

## Prompt Summary

Refactor `infrastructure/` from flat Docker-centric layout into enterprise-grade, domain-structured architecture with clear separation of concerns and multi-environment deployment support.

---

## Architecture — Before

```
infrastructure/
├── docker/
│   ├── kafka/
│   ├── monitoring/
│   ├── opa/
│   ├── postgres/
│   └── redis/
├── host/
│   └── Whycespace.FoundationHost/
├── kubernetes/
├── localdev/
├── persistence/
│   ├── capital-ledger/
│   └── workflow/
└── terraform/
```

## Architecture — After

```
infrastructure/
├── ci-cd/                          # placeholder — CI/CD pipelines
├── data/
│   ├── postgres/                   # was docker/postgres
│   └── redis/                      # was docker/redis
├── deployment/
│   ├── kubernetes/                 # was kubernetes/
│   └── terraform/                  # was terraform/
├── environments/
│   └── local/                      # was localdev/
├── messaging/
│   └── kafka/                      # was docker/kafka
├── networking/                     # placeholder — network config
├── observability/
│   └── prometheus/                 # was docker/monitoring
├── persistence/
│   ├── Whycespace.Infrastructure.Persistence.csproj
│   └── stores/
│       ├── capital/                # was capital-ledger/
│       ├── eventstore/             # PostgresEventStore, ProjectionStore
│       └── workflow/               # all workflow stores + repository
├── policy/
│   └── opa/                        # was docker/opa
├── runtime/
│   └── host/
│       └── Whycespace.FoundationHost/  # was host/
└── security/                       # placeholder — security config
```

---

## File Changes

### Moves (git mv — preserves history)
| From | To |
|------|------|
| docker/postgres | data/postgres |
| docker/redis | data/redis |
| docker/kafka | messaging/kafka |
| docker/opa | policy/opa |
| docker/monitoring | observability/prometheus |
| host/Whycespace.FoundationHost/* | runtime/host/Whycespace.FoundationHost/* |
| kubernetes/ | deployment/kubernetes/ |
| terraform/ | deployment/terraform/ |
| localdev/ | environments/local/ |
| persistence/PostgresEventStore.cs | persistence/stores/eventstore/ |
| persistence/ProjectionStore.cs | persistence/stores/eventstore/ |
| persistence/WorkflowStateRepository.cs | persistence/stores/workflow/ |
| persistence/workflow/* | persistence/stores/workflow/* |
| persistence/capital-ledger/* | persistence/stores/capital/* |

### Reference Updates
| File | Change |
|------|--------|
| Whycespace.FoundationHost.csproj | All ProjectReference paths updated (+1 level) |
| Whycespace.slnx | Host project path updated to runtime/host/ |
| Dockerfile (FoundationHost) | COPY/restore/publish paths updated |
| docker-compose.yml | Volume mounts, build contexts, OPA build path updated |

### New Files
| File | Purpose |
|------|---------|
| security/.gitkeep | Placeholder for security module |
| networking/.gitkeep | Placeholder for networking module |
| ci-cd/.gitkeep | Placeholder for CI/CD module |

---

## Validation

- `Whycespace.Infrastructure.Persistence.csproj` — builds clean (0 errors)
- `Whycespace.FoundationHost.csproj` — only pre-existing namespace errors from broader system refactor (Downstream.Mobility → Downstream.Work.Mobility)
- No duplicate files
- All git history preserved via `git mv`
