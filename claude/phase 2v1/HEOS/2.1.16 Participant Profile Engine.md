# WHYCESPACE WBSM v3
# HEOS — HUMAN ECONOMIC OPERATING SYSTEM
# 2.1.16 PARTICIPANT PROFILE ENGINE

You are implementing the **Participant Profile Engine** inside the Whycespace system.

This engine belongs to **HEOS (Human Economic Operating System)** and manages updates and validation of **Participant Profile data**.

The engine must operate on the **Participant Aggregate** created in:

src/domain/core/participants/

This implementation must follow **WBSM v3 Prompt Structural Rules and Guardrails**.

---

# STEP 1 — ARCHITECTURE VERIFICATION

Component classification:

Participant Profile Engine = **Execution Engine**

Correct layer:

src/engines/T2E/

Reason:

The engine performs deterministic business operations on domain aggregates.

It does not orchestrate workflows.

It does not manage runtime scheduling.

Therefore it belongs in **T2E Execution Engines**.

---

# STEP 2 — TARGET FILE PATH

Create the HEOS execution engine module.

Target location:

src/engines/T2E/HEOS/

Files to create:

ParticipantProfileEngine.cs  
ParticipantProfileDecision.cs  
ParticipantProfileCommand.cs  

Verify:

• no domain models placed inside engines  
• no runtime logic inside engines  
• no direct engine-to-engine calls

---

# STEP 3 — TARGET DIRECTORY STRUCTURE

Confirm directory structure before generating code.

src/engines/T2E/HEOS/

ParticipantProfileEngine.cs  
ParticipantProfileCommand.cs  
ParticipantProfileDecision.cs  

If the directory does not exist, create it exactly as defined.

Do NOT create new top-level folders.

---

# STEP 4 — ENGINE INPUT MODEL

Create a command model.

ParticipantProfileCommand

Fields:

ParticipantId  
FirstName  
LastName  
Email  
PhoneNumber  
Country  

The command represents a request to update or create a participant profile.

Command objects must be immutable.

---

# STEP 5 — ENGINE OUTPUT MODEL

Create a decision model.

ParticipantProfileDecision

Fields:

Success  
Reason  
UpdatedProfile  

Decision models represent deterministic outcomes of engine execution.

---

# STEP 6 — ENGINE IMPLEMENTATION

Create:

ParticipantProfileEngine.cs

Responsibilities:

• validate participant profile data  
• apply profile updates to ParticipantAggregate  
• enforce domain rules  
• return a decision result

The engine must be:

stateless  
thread-safe  
deterministic  

The engine must NOT:

persist state  
call other engines  
invoke infrastructure  

The engine must operate only on:

domain aggregates  
system models  

Example method:

UpdateProfile(ParticipantAggregate participant, ParticipantProfileCommand command)

Return:

ParticipantProfileDecision

---

# STEP 7 — VALIDATION RULES

The engine must enforce the following rules:

Email must not be empty  
FirstName must not be empty  
LastName must not be empty  

Country must be valid ISO code.

Phone number must pass basic format validation.

Invalid requests must return a failure decision.

---

# STEP 8 — ENGINE INVOCATION RULE

The engine must never call another engine.

Correct invocation flow:

Runtime Dispatcher → ParticipantProfileEngine

Incorrect:

ParticipantProfileEngine → another engine

---

# STEP 9 — UNIT TESTS

Create deterministic tests.

Location:

tests/engines/

Create:

ParticipantProfileEngineTests.cs

Test scenarios:

successful profile update  
invalid email rejection  
missing name rejection  
phone validation failure  
profile update for suspended participant  

All tests must pass.

---

# STEP 10 — DEBUG INSPECTION

Expose a development endpoint.

Location:

src/platform/

Example endpoint:

POST /dev/heos/profile/update

This endpoint should call the runtime which then invokes the engine.

Platform must never call engines directly.

---

# STEP 11 — VALIDATION CHECKLIST

Before completing implementation verify:

No domain models created inside engines  
Engine is stateless  
No engine-to-engine calls  
Layer boundaries respected  
Aggregate used correctly  

---

# SUCCESS CRITERIA

Build succeeds  
0 warnings  
0 errors  
All tests pass  
Architecture remains unchanged